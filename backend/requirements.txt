# One file covers both local (Mock) and AWS (real OCR/AI/translate) modes.
fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
alembic==1.13.3
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.12
tzdata==2026.2          # GPS 라우터 ZoneInfo("Asia/Seoul") — Windows는 IANA tz DB 없어 필수

# AWS mode (PROVIDER_MODE=aws): Bedrock / Translate / Upstage
boto3==1.35.30          # Bedrock (Claude Vision/Text), Amazon Translate
requests==2.32.3        # Upstage Document Parse API + 소셜 OAuth 토큰 교환

# Tests (pytest + FastAPI TestClient needs httpx)
pytest==8.3.3
httpx==0.27.2
prometheus_client==0.21.0  # /metrics 엔드포인트 (Prometheus scrape)

# RDS Postgres + pgvector RAG.
psycopg[binary]==3.2.3
pgvector==0.3.6

# RAG source extraction utilities for official PDF/HWP files.
pypdf==6.13.2
olefile==0.47

python-json-logger>=2.0.0
aws-xray-sdk>=2.12.0
