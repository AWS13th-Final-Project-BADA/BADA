# Rule engine runs on stdlib only (testable without these).
# The packages below are for the full worker runtime.
# NOTE: weasyprint needs GTK libs on Windows - install at W3, not required for tests.
boto3==1.35.30          # Bedrock (Claude Vision/Text)
requests==2.32.3        # Upstage Document Parse API
pydantic==2.9.2         # OCR output schema validation
pydantic-settings==2.5.2  # backend/app/config.py 의존성(Worker DB 직접 접근)
jinja2==3.1.4
pytest==8.3.3
hypothesis==6.115.0     # Property-Based Testing
shapely==2.0.6          # local geofence (polygon). Prod uses PostGIS ST_Contains.
weasyprint==63.0        # PDF render. Windows: needs GTK runtime (W3 only).
sqlalchemy==2.0.35      # DB direct access (Stage 2)
psycopg[binary]==3.2.3  # PostgreSQL driver
aws-xray-sdk>=2.12.0
prometheus-client>=0.20.0
